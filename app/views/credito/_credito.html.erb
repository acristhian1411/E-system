
<%= semantic_form_for(CreditoCliente.new, :url => { :action => "create" }) do |f| %>

    <%= f.input :venta_id %><br>
    <%= f.input :cliente_id %>

<%=    f.template.render partial: 'cuotas/cuota' %>


<% end %>
